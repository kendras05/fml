<%= form_with(url: login_path, method: :post, local: true) do |f| %>
  <div>
    <%= f.label(:email) %><br>
    <%= f.text_field(:email) %>
  </div>
  <div>
    <%= f.label(:password) %><br>
    <%= f.password_field(:password) %>
  </div>
  <%= f.submit("Login") %>
<% end %>

